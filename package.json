{
  "name": "pdf-reader",
  "version": "1.2.0",
  "description": "Local PDF and EPUB audiobook reader powered by Kokoro TTS",
  "type": "module",
  "scripts": {
    "setup": "bash scripts/setup-tts.sh",
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-http": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
